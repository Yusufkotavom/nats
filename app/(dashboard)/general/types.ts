export type Customer = {
  name: string;
  email: string;
  phone: string;
  address: string;
  isActive: boolean;
};

export type Vendor = {
  name: string;
  email: string;
  phone: string;
  address: string;
  isActive: boolean;
};
